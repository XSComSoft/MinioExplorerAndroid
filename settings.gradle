pluginManagement {
    repositories {
        mavenLocal()
        maven{ url 'https://xssoft.club:5678/repository/main'}
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        maven{ url 'https://xssoft.club:5678/repository/main'}
        gradlePluginPortal()
    }
}

rootProject.name = "FileBrowser"
include ':app'
